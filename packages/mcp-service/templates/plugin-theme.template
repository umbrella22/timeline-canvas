import type { TimelinePlugin } from "../../plugins/types";
import type { TimelineColors } from "../../types";
import { PluginType } from "../../plugins/types";

const colors: Partial<TimelineColors> = {
  // TODO: define theme colors
  canvasBackground: "#ffffff",
  timelineText: "#000000",
};

export const {{EXPORT_NAME}}: TimelinePlugin = {
  metadata: {
    name: {{METADATA_NAME_LITERAL}},
    version: {{VERSION_LITERAL}},
    description: {{DESCRIPTION_LITERAL}},
    type: PluginType.THEME,
  },
  activate(context) {
    // Apply theme colors to the timeline
    context.config.colors = { ...context.config.colors, ...colors };
  },
  deactivate(_context) {
    // Theme cleanup is handled by the plugin system
  },
};
