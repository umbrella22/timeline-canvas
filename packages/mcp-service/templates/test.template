import { describe, it, expect } from "vite-plus/test";
import { {{EXPORT_NAME}} } from "../src/plugins/builtin/{{EXPORT_NAME}}";

{{#IF factoryPlugin}}
const plugin = {{EXPORT_NAME}}();
{{/IF}}
{{#UNLESS factoryPlugin}}
const plugin = {{EXPORT_NAME}};
{{/UNLESS}}

describe({{EXPORT_NAME_LITERAL}}, () => {
  it("should have correct metadata", () => {
    expect(plugin.metadata).toMatchObject({
      name: {{METADATA_NAME_LITERAL}},
      version: {{VERSION_LITERAL}},
      description: {{DESCRIPTION_LITERAL}},
    });
  });
});
