{
	"servers": {
		"timeline-canvas": {
			"type": "stdio",
			"command": "npx",
			"cwd": "${input:cwd}",
			"args": [
				"-y",
				"timeline-canvas-mcp@latest",
			],
			"env": {
				"MCP_WORKSPACE_ROOT": "${input:cwd}"
			}
		}
	},
	"inputs": [
		{
			"id": "cwd",
			"type": "promptString",
			"description": "Working directory (cwd)"
		}
	]
}