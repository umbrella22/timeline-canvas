### 1. 上帝类 `Timeline` 已经严重阻碍可维护性

**问题定位**：`src/Timeline.ts` 接近 1600 行，承担了渲染调度、事件管理、缩放滚动、命中测试、插件执行、生命周期控制等十余项职责。

**具体危害**：

- 任何修改都可能意外影响不相关的功能，修复一个 bug 往往会引入另一个。
- 无法对核心逻辑（如渲染脏区计算、事件坐标转换）进行单元测试。
- 新贡献者看到这个类会直接放弃理解。

**改进建议**（按优先级）：

1. **立即拆分出独立的状态机**：将 `dragState`、`zoomState`、`selectState` 等交互状态抽取为 `InteractionStateMachine` 类。
2. **分离渲染引擎**：将 `drawGrid`、`drawTracks`、`drawItems` 等所有绘图逻辑移到 `CanvasRenderer` 类中，`Timeline` 只持有渲染器实例。
3. **抽离事件坐标转换逻辑**：将 `getTimeFromX`、`getXFromTime` 等转换函数集中到 `ViewportCalculator`。
4. **使用依赖注入**：让 `Timeline` 通过构造函数接收这些拆分后的模块，而不是自己 new 一切。

**验收标准**：拆分后 `Timeline` 主类不超过 400 行，且每个模块都有独立的单元测试。

---

### 2. 零测试覆盖率：自称“高性能可靠”但没有任何保障

**问题定位**：`package.json` 中 `"test": "echo \"Error: no test specified\""`，整个项目无任何单元测试或集成测试。

**具体危害**：

- 任何重构都是盲人摸象，你无法保证修改不破坏现有行为。
- 外部用户不敢在生产环境使用，因为没有回归测试的保障。
- 无法通过 CI 自动化验证 PR。

**改进建议**：

1. **建立快照测试**：使用 `jest` 或 `vitest` + `jest-canvas-mock`，对关键渲染输出（网格、轨道、事件矩形）进行快照对比。
2. **核心算法必测**：对 `hitTest`（命中检测）、`getVisibleRange`（视口计算）、`layoutEvents`（防碰撞布局）编写纯函数单元测试。
3. **添加集成测试**：模拟用户交互（鼠标拖拽缩放、滚动）并断言 `onViewportChange` 回调是否被正确触发。
4. **设置 CI 门禁**：在 GitHub Actions 中运行 `npm test`，要求覆盖率 > 80% 才允许合并。

**验收标准**：至少完成 30 个以上单元测试，核心工具函数覆盖率达到 100%。

---

### 3. TypeScript 类型安全形同虚设：大量 `any` 侵蚀了 TS 的所有优势

**问题定位**：根据你自检文档中承认的，插件 API 和很多公共方法使用了 `any`。快速扫描可见 `src/plugins/index.ts` 及事件回调参数。

**具体危害**：

- 编辑器失去智能提示，开发者被迫频繁阅读源码才能知道参数结构。
- 运行时才能发现类型错误，完全违背使用 TS 的初衷。
- 插件开发者难以正确实现接口，容易导致崩溃。

**改进建议**：

1. **定义精确的插件类型**：

   ```typescript
   interface TimelinePlugin {
     name: string;
     setup?: (ctx: PluginContext) => void;
     render?: (ctx: RenderContext) => void;
     // 禁止使用 any
   }
   ```

2. **启用 `strict` 模式**：在 `tsconfig.json` 中开启 `"strict": true`，然后逐一修复所有 `any`。
3. **为所有公共 API 导出类型**：在 `index.ts` 中显式导出 `TimelineConfig`、`TimelineEvent`、`PluginContext` 等类型供外部使用。
4. **添加 `tsc --noEmit` 到 CI**：确保任何 PR 都不会引入新的类型漏洞。

**验收标准**：项目中 `any` 出现次数降为 0（合理场景可使用 `unknown` 并做类型收窄）。

---

### 4. 国际化硬编码：与“通用组件”的定位矛盾

**问题定位**：`src/utils/format.ts` 中的 `formatDuration` 函数直接输出“X天X小时X分钟”，而 `formatTime` 也是中文格式。

**具体危害**：

- 英文环境或非中文用户直接看到乱码般的汉字。
- 无法被用于跨国项目或 UI 库。

**改进建议**：

1. **抽离所有文本资源**：

   ```typescript
   const defaultLocale = {
     day: "d",
     hour: "h",
     minute: "m",
     second: "s",
   };
   ```

2. **支持通过配置传入自定义格式化函数**：

   ```typescript
   interface TimelineConfig {
     formatDuration?: (ms: number) => string;
   }
   ```

3. **内置中英文两种语言**，并通过 `locale` 配置切换。

**验收标准**：在英文环境下，`formatDuration(86400000)` 输出 `1d` 而不是 `1天`。

---

### 5. 性能魔法数字被写死，剥夺用户调优能力

**问题定位**：代码中存在 `EDGE_SCROLL_THRESHOLD = 20`、`ZOOM_SENSITIVITY = 0.001`、`RENDER_DEBOUNCE = 16` 等常量。

**具体危害**：

- 在不同屏幕尺寸、不同数据量下，最优的阈值完全不同。用户无法适配自己的场景。
- 你的默认值可能只适用于你自己的测试环境。

**改进建议**：

1. **将这些常量暴露为可配置项**：

   ```typescript
   interface PerformanceConfig {
     edgeScrollThreshold?: number; // default 20
     zoomSensitivity?: number; // default 0.001
     renderDebounceMs?: number; // default 16
   }
   ```

2. **在文档中说明每个参数的作用和调优场景**（如高刷新率屏幕需要更小的 debounce）。

**验收标准**：用户可以在初始化时传入 `{ edgeScrollThreshold: 30 }` 直接覆盖默认值。

---

### 6. 开源协作基础设施缺失：你实际上拒绝了社区贡献

**问题定位**：仓库没有 `CONTRIBUTING.md`、`Issue 模板`、`PR 模板`，也没有 `CODEOWNERS` 或自动化测试。

**具体危害**：

- 外部开发者想提交 bug 修复，不知道该遵循什么流程。
- 你收到的 issue 会缺少环境信息、复现步骤，浪费大量来回沟通时间。
- 没有 `pull_request_template`，PR 描述往往不完整。

**改进建议**：

1. **创建 `.github/ISSUE_TEMPLATE/bug_report.md`**：要求填写版本、浏览器、复现步骤、最小 demo 链接。
2. **创建 `.github/PULL_REQUEST_TEMPLATE.md`**：包含检查清单（是否添加测试、是否更新文档、是否通过 lint）。
3. **编写 `CONTRIBUTING.md`**：说明如何本地启动、如何运行测试（未来）、代码风格、commit 规范。
4. **添加 `CODE_OF_CONDUCT.md`**：虽然不是吸引贡献的核心，但表明你认真对待社区。

**验收标准**：任何新访客在 5 分钟内可以明白如何提交第一个 PR。

---

### 7. MCP 集成值得肯定，但缺乏实际使用案例和文档

**问题定位**：`mcp-server/` 目录实现了 MCP 服务，但 README 中没有说明 AI 助手如何调用它，也没有示例对话。

**具体危害**：

- 这个超前功能变成了“自我满足的技术玩具”，没有给用户带来实际价值。
- 外部用户不知道怎么用，也不会去探索。

**改进建议**：

1. **在 README 中增加一节 “AI 辅助使用”**：
   - 展示如何配置 Claude Desktop 或 Continue 连接到你的 MCP 服务。
   - 给出一个示例 prompt：“调整时间轴的起始时间为 2025-01-01” 并展示 AI 通过 MCP 调用 API 的过程。
2. **为 MCP 服务编写端到端测试**：确保 API 契约稳定。

**验收标准**：一个新用户能在 10 分钟内让 AI 助手成功操作时间轴组件。

---

### 总结：最具“杠杆效应”的下一步行动

如果你想用最少的时间获得最大的质量提升，我建议按以下顺序处理：

1. **添加测试**（2 天）—— 这是建立信心的基石。
2. **修复类型 any**（1 天）—— 极大改善开发体验。
3. **拆分上帝类的渲染部分**（3 天）—— 让后续所有修改都更安全。
4. **暴露性能配置项**（半天）—— 解决用户痛点最直接。
5. **完善社区文档**（1 天）—— 为吸引贡献者铺路。

完成这些后，你完全可以自豪地将项目版本号升至 `1.0.0`，并开始真正的推广。届时如果社区活跃度依然为零，那才需要担心——但现在，先让项目内部质量配得上你的设计野心。

期待看到 `timeline-canvas` 成为一个被广泛使用的优秀组件。

1. **在 README 中增加一节 “AI 辅助使用”**：
   - 展示如何配置 Claude Desktop 或 Continue 连接到你的 MCP 服务。
   - 给出一个示例 prompt：“调整时间轴的起始时间为 2025-01-01” 并展示 AI 通过 MCP 调用 API 的过程。
2. **为 MCP 服务编写端到端测试**：确保 API 契约稳定。

**验收标准**：一个新用户能在 10 分钟内让 AI 助手成功操作时间轴组件。

---

### 总结：最具“杠杆效应”的下一步行动

如果你想用最少的时间获得最大的质量提升，我建议按以下顺序处理：

1. **添加测试**（2 天）—— 这是建立信心的基石。
2. **修复类型 any**（1 天）—— 极大改善开发体验。
3. **拆分上帝类的渲染部分**（3 天）—— 让后续所有修改都更安全。
4. **暴露性能配置项**（半天）—— 解决用户痛点最直接。
5. **完善社区文档**（1 天）—— 为吸引贡献者铺路。

完成这些后，你完全可以自豪地将项目版本号升至 `1.0.0`，并开始真正的推广。届时如果社区活跃度依然为零，那才需要担心——但现在，先让项目内部质量配得上你的设计野心。

期待看到 `timeline-canvas` 成为一个被广泛使用的优秀组件。
